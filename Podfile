# Uncomment this line to define a global platform for your project
platform :ios, '8.0'
use_frameworks!

target 'Biky' do
  pod "AFNetworking", "~> 2.0"
  pod "Fabric"
  pod "Crashlytics"
end

target 'BikyTests' do
  pod "AFNetworking", "~> 2.0"
end
